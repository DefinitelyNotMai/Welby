const useRedirect = () => {
  //const { isLoggedIn } = useUserContext();
  // const navigate = useNavigate();
  // useEffect(() => {
  //   if (isLoggedIn) {
  //     navigate("/dashboard");
  //   } else {
  //     navigate("/");
  //   }
  // }, [isLoggedIn, navigate]);
};

export default useRedirect;
